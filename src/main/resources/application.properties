spring.application.name=app

server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/ecommerce_db
spring.datasource.username=root
spring.datasource.password=micheal2001

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

secretJWtString=${JWT_SECRET_STRING}

cloudinary.cloud-name=${cloudName}
cloudinary.api-key=${cloudinaryApiKey}
cloudinary.api-secret=${cloudinaryApiSecret}

server.servlet.session.cookie.name=GUEST_SESSION_ID
server.servlet.session.timeout=30m

# ==============================
# ? ADMIN PANEL (optional)
# ==============================
# If you later add Spring Security:
# admin.default.username=admin
# admin.default.password=secret


